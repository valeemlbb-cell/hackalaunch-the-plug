# state, secrets and generated media - never committed
workspace/
.env
data/contacts.local.json
*.eml
demo_build/

# demo_small.mp4 (720p, 2.4 MB) IS committed. demo.mp4 (1080p) is NOT - it goes on a
# GitHub release instead, so the tree does not carry two near-identical videos. RUN.md 3.
demo.mp4

# internal pre-submission review notes - not part of the submission
REVIEW_*.md

__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
.vscode/
.idea/
.DS_Store
